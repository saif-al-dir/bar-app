// selectors (to be added later)

// actions
// const createActionName = actionName => `app/tables/${actionName}`;

// action creators (to be added later)

// reducer
const tablesReducer = (statePart = [], action) => {
    switch (action.type) {
        default:
            return statePart;
    }
};

export default tablesReducer;
